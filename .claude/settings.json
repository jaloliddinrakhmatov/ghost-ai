{
  "permissions": {
    "allow": [
      "Bash(node -e \"const raw = process.env.DATABASE_URL; if (!raw) { process.exit(1); } const u = new URL(raw); console.log(u.searchParams.get('sslmode') || u.searchParams.get('ssl'))\")"
    ]
  }
}
